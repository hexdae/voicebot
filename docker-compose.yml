version: '3.7'

services:
  bot:
    build:
      context: .
    volumes:
      - ".:/app"
    ports:
      - '5000:5000'
    command: ["bash"]
    # command: ["bash", "-c", "source chatbot-venv/bin/activate && python bot.py"]
